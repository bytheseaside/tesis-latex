\nota{aca iria el codigo del solver/ la referencia a donde pongamos el codigo (appendix? ver como lo organizamos) y decir un poco de sarasa de que implementa la ecuacion deducida arriba + un poco de sarasa de decision}


\subsubsection{step view (visor)}
\nota{Aca habria que poner algunas fotos del viewer explicar que hace etc etc. es minor, pero suma}



\subsubsection{step probe (medicion de resultados)}
\nota{Aca habria que poner algunas fotos del viewer explicar que hace etc etc. es minor, pero suma}